name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    services:
      mailpit:
        image: axllent/mailpit:v1.27
        ports: ["1025:1025", "8025:8025"]
    env:
      MONGODB_URI: mongodb://127.0.0.1:27017/votiy?replicaSet=rs0
      MONGODB_TEST_URI: mongodb://127.0.0.1:27017/?replicaSet=rs0
      APP_ORIGIN: http://127.0.0.1:5173
    steps:
      - uses: actions/checkout@v4
      - name: Start MongoDB replica set
        run: |
          docker run -d --name votiy-ci-mongodb -p 27017:27017 mongo:8.0 mongod --replSet rs0 --bind_ip_all
          until docker exec votiy-ci-mongodb mongosh --quiet --eval "db.adminCommand('ping').ok"; do sleep 2; done
          docker exec votiy-ci-mongodb mongosh --quiet --eval "rs.initiate({_id:'rs0',members:[{_id:0,host:'127.0.0.1:27017'}]})"
          until docker exec votiy-ci-mongodb mongosh --quiet --eval "quit(db.adminCommand('hello').isWritablePrimary ? 0 : 1)"; do sleep 2; done
      - uses: pnpm/action-setup@v4
        with:
          version: 11.7.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: |
            pnpm-lock.yaml
            votiy-web/pnpm-lock.yaml
            votiy-api/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - run: pnpm --dir votiy-web install --frozen-lockfile
      - run: pnpm --dir votiy-api install --frozen-lockfile
      - run: pnpm format:check
      - run: pnpm --dir votiy-web lint
      - run: pnpm --dir votiy-api test:coverage
      - run: pnpm --dir votiy-web test:coverage
      - run: pnpm --dir votiy-api test:contract
      - run: pnpm --dir votiy-api test:integration
      - name: Verify voting rules, access, code races, privacy, and contracts
        run: |
          pnpm --dir votiy-api exec vitest run tests/unit/event-voting-rules.test.js tests/unit/ballot-submission.test.js tests/unit/voter-eligibility.test.js tests/unit/voting-code-management.test.js
          pnpm --dir votiy-api exec vitest run tests/contract/event-voting-rules.contract.test.js tests/contract/event-ballot.contract.test.js tests/contract/voter-access.contract.test.js tests/contract/voting-code-management.contract.test.js
          pnpm --dir votiy-api exec vitest run tests/integration/event-voting-rules.test.js tests/integration/event-ballot-submission.test.js tests/integration/voter-access.test.js tests/integration/voting-code-claim.test.js tests/integration/event-voting-observability.test.js
          pnpm --dir votiy-web exec vitest run tests/component/event-rules-editor.test.jsx tests/component/event-ballot.test.jsx tests/component/voter-access.test.jsx tests/component/voting-code-manager.test.jsx
      - name: Verify Add Entries contract and browser coverage
        run: |
          pnpm --dir votiy-api exec vitest run tests/contract/add-entries.contract.test.js
          pnpm --dir votiy-web exec vitest run tests/component/add-entry-modal.test.jsx tests/component/add-entry-owner-choices.test.jsx tests/component/add-entry-owner-search.test.jsx tests/component/add-entry-provisional-owner.test.jsx
      - name: Verify category editing and archival coverage
        run: |
          pnpm --dir votiy-api exec vitest run tests/contract/edit-entry-titles.contract.test.js tests/unit/update-event-category-validation.test.js tests/unit/update-event-category-service.test.js tests/contract/remove-category.contract.test.js tests/contract/category-archival-persistence.contract.test.js tests/unit/archive-event-category-validation.test.js tests/unit/archive-event-category-service.test.js tests/unit/category-archival-domain.test.js
          pnpm --dir votiy-web exec vitest run tests/component/edit-entry-titles.test.jsx tests/component/remove-category.test.jsx tests/component/event-setup-view.test.jsx
      - run: pnpm --dir votiy-web build
      - run: node --check votiy-api/src/server.js
      - run: node --check tests/smoke/production-smoke.js
      - run: pnpm exec playwright install --with-deps chromium
      - run: pnpm test:e2e --project=chromium
      - run: pnpm test:e2e --project=mobile-chromium --grep "responsive|public shell"
      - run: pnpm test:e2e tests/e2e/event-voting-rules.spec.js tests/e2e/responsive-accessibility.spec.js
