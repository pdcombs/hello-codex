# UI Contract

Read-only event entry row displays entry title only. It does not display owner attribution. Editable entry field keeps accessible label containing owner context. Remove control behavior remains unchanged.
